<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is off by default from Android 9 (API 28). The LMS API is
  currently served over plain http on the LAN, so the dev hosts are allowed
  explicitly here rather than switching cleartext on for every domain.

  Add your machine's IP when it changes, and drop this file once the API is
  reachable over https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- API host used by ApiConstants.baseUrl (laptop LAN IP) -->
        <domain includeSubdomains="false">10.243.237.169</domain>

        <!-- Previous API host -->
        <domain includeSubdomains="false">10.50.2.130</domain>

        <!-- Previous API host -->
        <domain includeSubdomains="false">10.154.22.71</domain>

        <!-- Previous API host -->
        <domain includeSubdomains="false">10.147.155.61</domain>

        <!-- Android emulator loopback to the host machine -->
        <domain includeSubdomains="false">10.0.2.2</domain>

        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
